"""HTTP API for the face-recognition service (FastAPI + Uvicorn).

The :mod:`core` package provides the algorithms; this package only handles
HTTP plumbing — routing, request/response schemas, image en/decoding, and
service health checks.
"""
